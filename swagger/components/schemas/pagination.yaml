components:
  schemas:
    pagination:
      type: object
      properties:
        current_page:
          type: integer
         #example: 1
        per_page:
          type: integer
         # example: 10
        count:
          type: integer
          #example: 250
        total_pages:
          type: integer
          #example: 25
